// Re-export of the shared AssetRefCell. The component was promoted to
// `components/shared/` once tag-review started reusing it; this thin file is
// kept so existing governance tab imports keep working without a sweep.
export { AssetRefCell } from "@/components/shared/AssetRefCell";
