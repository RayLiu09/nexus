# NEXUS Console 环境变量示例（非密钥，不要提交真实 key）
# 在本地开发时复制为 `.env.local` 并按实际填写。

# 后端 API 基础地址（不带末尾斜杠）。默认指向本地 nexus-api。
NEXUS_API_BASE_URL=http://127.0.0.1:8000

# 检索/QA 演示入口使用的 api_caller key（仅在服务端读取，永不下发到浏览器）。
# 必须是 `api_caller` 表中已注册且未过期的 key；缺失时 /search 页面会返回 503。
NEXUS_DEMO_CALLER_KEY=
